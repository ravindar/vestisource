<!DOCTYPE html>
<html>
    <head>
        <title>Vestisource: Product Management</title>
        <!-- START:stylesheet -->
		<%= stylesheet_link_tag "scaffold" %>
        <%= stylesheet_link_tag "vestisource", :media => "all" %>
		<!-- <label id="code.slt"/> -->
		<!-- END:stylesheet -->
		<%= javascript_include_tag :defaults %>
        <%= csrf_meta_tag %>
    </head>
    <body id="store">
        <div id="banner">
            <a href="."><%= image_tag("logo.png") %></a>
            <%= @page_title || "Vestisource" %>
        </div>
        <div id="columns">
            <div id="side">
            	<!--<% if @cart %>
            		<%= hidden_div_if(@cart.line_items.empty?, :id => "cart" ) do %>
						<%= render @cart %>
					<% end %>
				<% end %>-->
                <a href=".">Home</a>
                <br/>
                <%= link_to "Products", products_path %>
                <br/>
                <a href="http://www..../faq">Questions</a>
                <br/>
                <a href="http://www..../news">News</a>
                <br/>
                <a href="http://www..../contact">Contact</a>
                <br/>
            </div>
            <div id="main">
                <%= yield %><!-- <label id="code.depot.e.include"/> -->
            </div>
        </div>
    </body>
</html>